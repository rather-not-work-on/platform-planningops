# planningops/scripts/federation

## Purpose
Keep cross-repository execution entrypoints isolated from the planningops core loop.

## Contents
- `federated_python_env.py`: deterministic local Python bootstrap helper for sibling-repo federated checks
  - prunes macOS `._*` metadata inside the managed venv and refuses to reuse an unhealthy interpreter
- `adapter_registry.py`: repo-to-adapter mapping registry for execution hooks
- `cross_repo_conformance_check.py`: federated contract/provider/o11y/runtime conformance matrix
  - clears any pre-existing per-run `run_root` before generating evidence so rerunning the same `run_id` cannot reuse stale monday idempotency or transition-log artifacts
- `test_contract_conformance_helper_wiring.sh`: regression that keeps the local federated matrix `contract-conformance` block wired to the canonical conformance helper
  - contract-conformance local entrypoint must call only `planningops/scripts/run_contract_conformance_ci_check.sh`
- `test_platform_contracts_helper_wiring.sh`: regression that keeps the GitHub `contract-conformance` job wired to the canonical platform-contracts helper
  - contract-conformance workflow entrypoint must call only `planningops/scripts/run_platform_contracts_ci_check.sh`
- `test_federated_conformance_helper_wiring.sh`: regression that keeps the GitHub `federated-conformance` job wired to the canonical workflow helper
  - federated-conformance workflow entrypoint must call only `planningops/scripts/run_federated_conformance_ci_check.sh`
  - the workflow job must not inline repo requirement installs; managed bootstrap belongs to the helper/checker path
- `test_federated_summary_helper_wiring.sh`: regression that keeps the GitHub `federated-summary` job wired to the canonical summary wrapper
  - federated-summary workflow entrypoint must call only `planningops/scripts/run_federated_summary_ci_check.sh`
- `test_cross_repo_conformance_run_root_reuse_contract.sh`: regression that reruns `cross_repo_conformance_check.py` with the same `run_id` and proves the temporary conformance run-root is rebuilt cleanly before monday scheduler evidence is regenerated
- `github_sync_adapter.py`: GitHub sync smoke/idempotency/drift helper
- `multi_repo_projection_report.py`: repo-level projection drift aggregation
- `run_issue_driven_mission_smoke.py`: planningops-owned bridge from GitHub issue context into monday local runtime smoke
- `run_issue_driven_runtime_stack_smoke.py`: planningops-owned aggregate runner that links an issue-driven monday mission smoke with provider and observability repo-owned smoke evidence
- `run_local_runtime_stack_smoke.py`: planningops-owned federated local smoke runner that cold-start primes provider/observability launchers before executing the monday smoke, then validates repo-owned provider/o11y evidence
- `test_supervisor_handoff_to_monday_status_bridge.sh`: cross-repo regression that ensures a blocked planningops supervisor handoff preserves federated `headline`, `first_action_command`, and remediation commands when monday translates it into operator-message CLI payloads, local dispatch packets, and canonical priority preview/display packets plus the planningops handoff validation sidecar
- `test_supervisor_handoff_to_monday_goal_completion_bridge.sh`: cross-repo regression that ensures a planningops goal-completion handoff preserves federated CTA fields when monday translates it into terminal goal-completion delivery, local dispatch packets, and canonical priority preview/display packets plus the planningops handoff validation sidecar
- `test_supervisor_handoff_to_monday_goal_completion_scheduler_bridge.sh`: cross-repo regression that ensures a planningops goal-completion handoff preserves federated CTA fields through monday scheduled admission, scheduler evidence, and canonical priority preview/display packets plus the planningops handoff validation sidecar
- `test_supervisor_handoff_bridge_wiring.sh`: regression that keeps both the local federated matrix and GitHub runtime-handoff job wired to the supervisor handoff bridge test
  - runtime-handoff local/workflow entrypoints must call only `planningops/scripts/run_runtime_handoff_ci_check.sh`
  - also keeps all three bridge regressions pinned to monday's canonical `resolve_operator_priority_preview.py` and `resolve_operator_priority_display_packet.py` consumer paths
  - plus planningops' canonical `resolve_supervisor_operator_handoff_validation.py`, `resolve_supervisor_operator_handoff_bundle.py`, `validate_supervisor_operator_handoff_bundle.py`, `assess_supervisor_operator_handoff_bundle_readiness.py`, `validate_supervisor_operator_handoff_bundle_readiness.py`, `doctor_supervisor_operator_handoff_bundle.py`, and `gate_supervisor_operator_handoff_bundle.sh` consumer paths
  - and requires each bridge to assert that multiple monday artifacts dereference the same canonical `operator_handoff_validation_path`, `priority_preview_ref`, and `priority_display_packet_ref`, both individually and through the shared bundle resolver, validation sidecar, readiness surface, and doctor/gate surface
- `test_provider_profile_helper_wiring.sh`: regression that keeps both the local federated matrix and GitHub `provider-profile` job wired to the canonical provider helper
  - provider-profile local/workflow entrypoints must call only `planningops/scripts/run_provider_profile_ci_check.sh`
- `test_provider_gateway_ready_helper_wiring.sh`: regression that keeps the local federated matrix `provider-gateway-ready` block wired to the canonical readiness helper
  - provider-gateway-ready local entrypoint must call only `planningops/scripts/run_provider_gateway_ready_ci_check.sh`
- `test_runtime_operations_ready_helper_wiring.sh`: regression that keeps the local federated matrix `runtime-operations-ready` block wired to the canonical runtime helper
  - runtime-operations-ready local entrypoint must call only `planningops/scripts/run_runtime_operations_ready_ci_check.sh`
- `test_o11y_replay_helper_wiring.sh`: regression that keeps both the local federated matrix and GitHub `o11y-replay` job wired to the canonical observability helper
  - o11y-replay local/workflow entrypoints must call only `planningops/scripts/run_o11y_replay_ci_check.sh`
- `test_external_artifact_residency_helper_wiring.sh`: regression that keeps the GitHub `loop-guardrails` job wired to the canonical external-artifact residency helper
  - the workflow subsection must call only `planningops/scripts/run_external_artifact_residency_ci_check.sh`
- `test_control_plane_governance_helper_wiring.sh`: regression that keeps the GitHub `loop-guardrails` job wired to the canonical control-plane governance helper
  - the workflow subsection must call only `planningops/scripts/run_control_plane_governance_ci_check.sh`
- `../run_runtime_handoff_ci_check.sh`: canonical helper for monday integration baseline plus supervisor handoff bridge regressions in local/workflow federated CI lanes
  - also replays `test_autonomous_supervisor_loop_contract.sh`, so the supervisor loop's `supervisor_handoff_common.py` dependency is sealed inside the same runtime-handoff lane
  - also replays `test_assess_supervisor_operator_handoff_bundle_readiness.sh` and `test_validate_supervisor_operator_handoff_bundle_readiness.sh`, so the handoff bundle-readiness assessor and validator remain sealed inside the same runtime-handoff lane
  - also replays the memory reflection path through `test_worker_outcome_reflection_cycle.sh`, `test_worker_outcome_reflection_cycle_scheduler_report.sh`, `test_reflection_goal_completion_handoff_cycle.sh`, `test_reflection_delivery_cycle.sh`, and `test_scheduled_reflection_delivery_cycle.sh`, so the terminal memory control-plane path is sealed inside the same runtime-handoff lane
  - also replays `test_reflection_cycle_orchestration_contract.sh`, `test_reflection_delivery_cycle_contract.sh`, and `test_scheduled_reflection_delivery_cycle_contract.sh`, so the reflection contract surface is sealed inside the same runtime-handoff lane
  - also replays `test_local_delivery_cycle_orchestration_contract.sh`, `test_scheduled_delivery_queue_admission_contract.sh`, `test_scheduled_queue_admission_handoff_contract.sh`, `test_scheduler_native_worker_outcome_selection_contract.sh`, and `test_scheduled_worker_outcome_handoff_contract.sh`, so the delivery and scheduler-admission contract surfaces are sealed inside the same runtime-handoff lane
  - also replays `test_reflection_action_handoff_contract.sh`, `test_scheduled_delivery_cycle_handoff_contract.sh`, `test_local_delivery_cycle_entrypoint_contract.sh`, `test_local_operator_target_resolution_contract.sh`, `test_local_outbox_dispatch_handoff_contract.sh`, and `test_local_dispatch_cycle_handoff_contract.sh`, so the downstream action-handoff and local delivery execution contract surfaces are sealed inside the same runtime-handoff lane
  - also replays `test_active_goal_registry_contract.sh` and `test_transition_goal_state_contract.sh`, so the active-goal and goal-transition policy consumed by the reflection action path is sealed inside the same runtime-handoff lane
  - also replays `test_supervisor_operator_handoff_contract.sh` and `test_validate_supervisor_operator_handoff_contract.sh`, so the supervisor handoff artifact and validation contract remain sealed inside the same runtime-handoff lane
  - also replays `test_federated_ci_summary_contract.sh` and `test_validate_federated_ci_summary_contract.sh`, so the canonical federated-summary artifact builder and validation contract stay sealed inside the same runtime-handoff lane
  - also replays `test_reconcile_federated_ci_summary_tmp.sh`, `test_validate_federated_ci_summary_tmp_reconcile_contract.sh`, and `test_resolve_federated_ci_summary_tmp_reconcile.sh`, so the root tmp-summary reconcile repair, validation, and resolution surface stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle.sh`, so the first tmp-summary reconcile bundle-status resolved bundle layer stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.sh`, so the next tmp-summary reconcile status-bundle-status outer doctor layer stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.sh`, so the next outer tmp-summary reconcile status-bundle-status-bundle-status layer stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, so the next outer tmp-summary reconcile status-bundle-status-bundle-status-bundle-status layer stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, so the next outer tmp-summary reconcile status-bundle-status-bundle-status-bundle-status-bundle-status layer stays sealed inside the same runtime-handoff lane
  - also replays `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh`, `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh`, `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh`, `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, and `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh`, so the next outer tmp-summary reconcile status-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status layer stays sealed inside the same runtime-handoff lane
  - also replays `test_assess_federated_ci_summary_readiness.sh`, `test_validate_federated_ci_summary_readiness_contract.sh`, `test_doctor_federated_ci_summary.sh`, and `test_gate_federated_ci_summary.sh`, so the canonical federated-summary readiness assessor, validator, diagnosis, and fail-closed gate stay sealed inside the same runtime-handoff lane
  - also replays `test_uap_automation_operations_summary_contract.sh`, so the operator-facing automation operations summary stays aligned with the same federated-CI readiness and supervisor handoff surfaces owned by the runtime-handoff lane
- `../run_platform_contracts_ci_check.sh`: canonical helper for the GitHub contract-conformance platform-contracts install, validation, and semver lane
  - seeds a repo-local managed virtualenv before dependency installation so local smoke runs stay compatible with externally managed Python interpreters
- `../run_contract_conformance_ci_check.sh`: canonical helper for the local federated contract-conformance bootstrap plus cross-repo checker run
- `../run_federated_conformance_ci_check.sh`: canonical helper for the GitHub federated-conformance artifact-policy rollout plus cross-repo checker run
  - forwards `--bootstrap-mode` to `cross_repo_conformance_check.py` so workflow runs can rely on the managed federated Python bootstrap instead of ad-hoc `pip install -r ...` steps
- `../run_federated_summary_ci_check.sh`: canonical helper for the GitHub federated-summary aggregation, validation, and readiness lane
- `../run_provider_profile_ci_check.sh`: canonical helper for provider launcher/profile dry-run checks in local/workflow federated CI lanes
- `../run_provider_gateway_ready_ci_check.sh`: canonical helper for the local federated provider-gateway-ready LiteLLM bootstrap plus readiness gate
- `../run_runtime_operations_ready_ci_check.sh`: canonical helper for the local federated monday runtime-operations-ready LiteLLM bootstrap plus readiness gate
- `../run_o11y_replay_ci_check.sh`: canonical helper for observability replay/backfill dry-run checks in local/workflow federated CI lanes
- `../run_external_artifact_residency_ci_check.sh`: canonical helper for the GitHub external-only commit/artifact residency subsection inside `loop-guardrails`
- `../run_control_plane_governance_ci_check.sh`: canonical helper for the GitHub artifact-policy / ontology / memory / inventory governance subsection inside `loop-guardrails`
- `run_wave14_oracle_rehearsal.py`: oracle rehearsal wrapper that reuses the federated stack smoke with deterministic simulated planner tasks so portability gaps are isolated from live planner credential drift
- `reflection_cycle_common.py`: shared repo/workspace/goal/bootstrap helpers consumed by the worker-outcome and scheduled reflection-cycle runners
- `run_worker_outcome_reflection_cycle.py`: planningops-owned runner that chains monday worker outcome or monday scheduler-evidence export into planningops reflection evaluation and action application
  - imports shared control-plane plumbing from `planningops/scripts/federation/reflection_cycle_common.py` instead of re-owning repo/workspace/goal helpers inline
- `../test_worker_outcome_reflection_cycle_scheduler_report.sh`: cross-repo regression that proves the reflection runner can start from a monday scheduler report for the memory consolidation queue path
- `run_reflection_goal_completion_handoff_cycle.py`: planningops-owned runner that turns a `goal_completed` reflection action into supervisor handoff artifacts plus monday goal-completion queue admission
  - imports shared control-plane plumbing from `planningops/scripts/federation/reflection_cycle_common.py` instead of re-owning repo/workspace/goal helpers inline
  - imports the shared handoff/report surface from `planningops/scripts/supervisor_handoff_common.py` instead of directly importing `autonomous_supervisor_loop.py`
- `../test_reflection_goal_completion_handoff_cycle.sh`: cross-repo regression that proves monday memory scheduler evidence can flow through planningops reflection, supervisor handoff validation, goal transition, and monday goal-completion scheduled admission
- `run_reflection_delivery_cycle.py`: planningops-owned runner that hands a reflection action artifact to monday scheduled delivery queue admission and writes one aggregate delivery-cycle report
  - imports shared control-plane plumbing from `planningops/scripts/federation/reflection_cycle_common.py` instead of re-owning repo/workspace/path helpers inline
- `run_scheduled_reflection_delivery_cycle.py`: planningops-owned runner that chains monday scheduled queue execution into reflection-cycle and then either delivery-cycle orchestration or the goal-completion handoff bridge for terminal memory completions
  - imports shared control-plane plumbing from `planningops/scripts/federation/reflection_cycle_common.py` instead of re-owning repo/workspace/goal helpers inline
- `run_local_oracle_rehearsal.py`: local-first vs oracle_cloud rehearsal harness
- `run_wave14_oracle_rehearsal.py`: wave-owned rehearsal runner that compares wave13 local vs oracle_cloud smoke outputs without changing the default local profile
- `validate_execution_wave_readiness.py`: readiness validator for plan waves that must verify prerequisite outputs and closed issues before projecting the next issue pack
- `review_interface_adoption.py`: spec-driven cross-repo review generator for completed interface waves, including file-marker and command-level validation
- `federated_ci_matrix_local.sh`: local federated CI matrix runner
  - now includes repo-local readiness gates for `platform-provider-gateway` and `monday` in addition to dry-run/profile drills
  - delegates tmp-summary checkpoint repair to `reconcile_federated_ci_summary_tmp.py` so a check command that clobbers the in-flight summary cannot silently collapse the stamped artifact to the last appended check
  - `provider-gateway-ready` and `runtime-operations-ready` now both delegate their LiteLLM bootstrap plus readiness gates to canonical helper entrypoints
  - local federated CI keeps `monday` on `PLANNER_RUNTIME_PROFILE=local`; free-only shells now rely on the checked-in LiteLLM fallback route instead of a separate `local_ollama` federated mode
  - `loop-guardrails` runs `run_track1_gate_dryrun.py` with `PLANNINGOPS_ALLOW_SCHEMA_FETCH_FAILURE=1` so local runs stay deterministic when GitHub project field fetch hits rate limits
  - `loop-guardrails` also audits inventory issue lifecycle from `planningops/fixtures/inventory-issue-lifecycle-audit-snapshot.json` instead of live `gh issue list`, so local matrix runs are stable when GitHub issue APIs are quota-limited
  - `loop-guardrails` now validates `planningops/config/plain-python-compat-manifest.json`, then lets GitHub replay the manifest-backed validator/doctor/gate/smoke chain through `planningops/scripts/run_plain_python_compat_workflow_chain.sh`; that helper resolves the canonical `workflow_command` list from `resolve_plain_python_compat_manifest.py` before running it, so the workflow no longer hand-inlines the raw plain-python compat step inventory
  - `loop-guardrails` also replays `planningops/scripts/test_plain_python_compat_manifest_wiring.sh`, so local lanes keep the raw manifest-backed order, GitHub lanes keep the helper-owned order, and both consumers stay pinned to the same canonical `resolve_plain_python_compat_manifest.py` inventory instead of hardcoded manifest paths
  - `loop-guardrails` now delegates issue-quality regressions plus the live `validate_issue_quality.py --strict` call to `planningops/scripts/run_issue_quality_ci_check.sh`, so the GitHub lane no longer hand-inlines the issue-quality subsection
  - `loop-guardrails` now delegates external-only commit/artifact residency regressions plus the live diff/tracked commit-guard audits to `planningops/scripts/run_external_artifact_residency_ci_check.sh`, so the GitHub lane no longer hand-inlines commit-guard fallback shell or residency checks
  - `loop-guardrails` now delegates artifact-policy, ontology, memory, inventory-lifecycle, and scheduler queue governance regressions to `planningops/scripts/run_control_plane_governance_ci_check.sh`, so the GitHub lane no longer hand-inlines those governance validators
  - `loop-guardrails` also replays `planningops/scripts/test_plain_python_compat_manifest_surface_inventory.sh`, so half-added recursive plain-python compat scripts or schemas fail before they can drift outside the contract and README inventory
  - `loop-guardrails` now preserves the pre-bootstrap shell `python3` path and replays `planningops/scripts/test_cross_repo_plain_python_compat.sh`, so plain-interpreter regressions in monday/observability/planningops entrypoints are caught before they break local federated runs
  - `loop-guardrails` also replays `planningops/scripts/test_runtime_stack_smoke_sequence_contract.sh`, so the exact issue-driven-runtime-stack-smoke -> local-runtime-stack-smoke sequence that previously exposed cross-repo interpreter drift stays covered through the same manifest-backed entrypoint set
  - the GitHub `loop-guardrails` job now checks out `monday` and `platform-observability-gateway` as well, so the same plain-python compat smoke runs in workflow parity with the local matrix
  - summary finalization now goes through `federated_ci_summary.py`, so interrupted runs still stamp a fail-closed summary instead of leaving only a `.tmp.json`
  - before each federated check, the runner snapshots the tmp summary and restores it if the check command clobbers `run_id`, `started_at_utc`, `required_checks`, or previously appended checks, so late-stage guardrails cannot silently downgrade the stamped summary to a one-check artifact
  - after summary finalization, the runner now regenerates stamped/latest summary-readiness artifacts so downstream automation never has to infer readiness from raw summary files
  - `runtime-handoff` now replays canonical bundle doctor/gate regressions in addition to resolve/validate/bridge coverage, so operator-facing handoff diagnosis stays wired into both local and GitHub federated lanes
  - `loop-guardrails` now replays canonical bundle readiness regressions in addition to the bundle validator/doctor/gate regressions, so operator-facing handoff readiness stays contract-checked even outside cross-repo bridge fixtures
- `federated_ci_summary.py`: helper that initializes, appends, and finalizes the federated CI summary artifact
  - shared by the local matrix runner and the GitHub `run_federated_summary_ci_check.sh` wrapper so both emit the same summary shape and failure classification
  - finalize now self-validates against `planningops/schemas/federated-ci-summary.schema.json` before stamping artifacts
  - finalize now supports separate stamped/latest validation outputs so canonical latest validation tracks the canonical latest summary path instead of the temporary build path
- `reconcile_federated_ci_summary_tmp.py`: repair helper for the local matrix tmp summary
  - restores the pre-check checkpoint when a check command deletes or rewrites the in-flight summary without preserving its canonical lineage fields or prior check history
  - local matrix now refreshes stamped/latest `federated-ci-summary-tmp-reconcile*.json` artifacts from this helper so repair evidence survives the run instead of living only in shell control flow
- `test_reconcile_federated_ci_summary_tmp.sh`: regression for healthy and restored tmp-summary checkpoint paths
- `validate_federated_ci_summary_tmp_reconcile.py`: validator for tmp-summary reconcile reports
  - fail-closes when reconcile reports drift from the canonical `healthy|restored` semantics or from the report output path that produced them
- `test_validate_federated_ci_summary_tmp_reconcile_contract.sh`: regression for valid and invalid tmp-summary reconcile reports
- `resolve_federated_ci_summary_tmp_reconcile.py`: canonical consumer for tmp-summary reconcile report + validation pairs
  - accepts either a reconcile report path or its validation report path and resolves one fail-closed bundle surface
- `test_resolve_federated_ci_summary_tmp_reconcile.sh`: regression for report-backed, validation-backed, and stale-validation tmp-summary reconcile bundle resolution
- `validate_federated_ci_summary_tmp_reconcile_bundle.py`: validator for tmp-summary reconcile bundle artifacts
  - fail-closes when the stored bundle drifts from canonical report+validation resolution or when its nested validation verdict is not `pass`
- `test_validate_federated_ci_summary_tmp_reconcile_bundle.sh`: regression for valid and conflicting tmp-summary reconcile bundle validation paths
- `doctor_federated_ci_summary_tmp_reconcile_bundle.py`: operator-facing diagnosis for the latest tmp-summary reconcile bundle plus its bundle-validation report
  - doctor treats a mismatched bundle-validation fingerprint as `tmp-summary reconcile bundle validation state: stale`
  - doctor now writes a canonical machine-readable status sidecar under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status.json`
  - doctor now also writes a canonical status-validation sidecar under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-validation.json`
  - doctor fails closed under `--require-pass` when tmp-summary reconcile bundle validation is stale, missing, or failing
- local matrix now also resolves and validates a canonical tmp-summary reconcile bundle status-bundle pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle.json` and `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-validation.json`
- local matrix now also resolves a canonical tmp-summary reconcile bundle status-bundle-status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle.json`
- local matrix now also validates that resolved tmp-summary reconcile bundle status-bundle-status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-validation.json`
- local matrix now also diagnoses that resolved tmp-summary reconcile bundle status-bundle-status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status.json` and `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-validation.json`
- local matrix now also resolves a canonical tmp-summary reconcile bundle status-bundle-status bundle status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle.json`
- local matrix now also validates that resolved tmp-summary reconcile bundle status-bundle-status bundle status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-validation.json`
- local matrix now also diagnoses that resolved tmp-summary reconcile bundle status-bundle-status bundle status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status.json` and `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-validation.json`
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical operator-facing diagnosis for that resolved status-bundle-status bundle status surface
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical status validator for that doctor-owned sidecar
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed gate wrapper for that doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the new status/status-validation contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.sh` keep the new doctor/gate lane pinned
- local matrix now also resolves a canonical tmp-summary reconcile bundle status-bundle-status bundle status bundle status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle.json`
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for that doctor-owned `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status.sh` keeps both entrypoints and the fail-closed stale-status-validation path pinned
- local matrix now also validates that resolved tmp-summary reconcile bundle status-bundle-status bundle status bundle status pair under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle-validation.json`
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical validator for that resolved bundle surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh` keeps the new resolved bundle-validation contract pinned
- local matrix now also diagnoses that resolved tmp-summary reconcile bundle status-bundle-status bundle status bundle status bundle under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status.json` and `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status-validation.json`
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical operator-facing diagnosis for that resolved bundle surface
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical status validator for that doctor-owned sidecar
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed gate wrapper for that doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the new status/status-validation contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` keep the new doctor/gate lane pinned
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for that doctor-owned `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh` keeps both resolver entrypoints and the fail-closed stale-validation path pinned
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical validator for that resolved doctor-owned status bundle surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh` keeps the new bundle-validation contract pinned
- local matrix now also diagnoses that outer resolved tmp-summary reconcile bundle surface under `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status.json` and `planningops/artifacts/validation/federated-ci-summary-tmp-reconcile-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status-bundle-status-validation.json`
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical operator-facing diagnosis for that outer resolved bundle surface
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical status validator for that doctor-owned sidecar
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed gate wrapper for that outer resolved bundle doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the new status/status-validation contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` keep the new doctor/gate lane pinned
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for that outer doctor-owned `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh` keeps both entrypoints and the fail-closed stale-status-validation path pinned
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical validator for that resolved outer doctor-owned bundle surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh` keeps the new resolved outer bundle-validation contract pinned
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical operator-facing diagnosis for the next outer resolved bundle surface
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical status validator for that next doctor-owned sidecar
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed gate wrapper for that next outer resolved bundle doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the next status/status-validation contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` keep the next doctor/gate lane pinned
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for that next doctor-owned `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh` keeps both entrypoints and the fail-closed stale-status-validation path pinned
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for the newly outer doctor-owned `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.sh` keeps both newly outer entrypoints and the fail-closed stale-status-validation path pinned
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical validator for that next outer resolved bundle
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh` keeps the next outer resolved bundle-validation contract pinned
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical operator-facing diagnosis for that next outer resolved bundle surface
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical status validator for that next outer doctor-owned sidecar
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed gate wrapper for that next outer resolved bundle doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the next outer status/status-validation contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` keep the next outer doctor/gate lane pinned
- `planningops/scripts/doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical prerequisite diagnosis surface for the following outer ring
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status.py` is the canonical prerequisite next-status validator above that newly prepared doctor surface
- `planningops/scripts/gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` is the fail-closed prerequisite gate wrapper for that newly prepared outer resolved bundle doctor surface
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_contract.sh` and `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_validation_contract.sh` keep the prerequisite next-status contracts pinned
- `planningops/scripts/test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` and `planningops/scripts/test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle_status_bundle.sh` keep the prerequisite doctor/gate lane pinned
- `../run_runtime_handoff_ci_check.sh` now also replays that prerequisite next-status contract, next-status validation contract, doctor, and gate quartet, so the newly prepared outer tmp-summary reconcile ring is sealed inside the same runtime-handoff lane
- `planningops/scripts/resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status.py` is the canonical consumer for that status-bundle-status bundle doctor `status + status-validation` pair
- `planningops/scripts/test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status.sh` keeps both entrypoints and the fail-closed stale-status-validation path pinned
- `planningops/scripts/validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle.py` is the canonical validator for that resolved status-bundle-status doctor bundle
- `planningops/scripts/test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_bundle_validation_contract.sh` keeps the new resolved bundle-validation contract pinned
- `test_doctor_federated_ci_summary_tmp_reconcile_bundle.sh`: regression for healthy, restored, and stale-validation tmp-summary reconcile bundle paths
- `gate_federated_ci_summary_tmp_reconcile_bundle.sh`: thin fail-closed gate wrapper around `doctor_federated_ci_summary_tmp_reconcile_bundle.py --require-pass`
- `test_gate_federated_ci_summary_tmp_reconcile_bundle.sh`: regression for passing and failing tmp-summary reconcile bundle gate paths
- `validate_federated_ci_summary_tmp_reconcile_bundle_status.py`: validator for the canonical tmp-summary reconcile bundle doctor status sidecar
  - fail-closes when the stored status drifts from canonical bundle doctor output or its recorded output path
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_contract.sh`: regression for valid and invalid tmp-summary reconcile bundle status documents
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_validation_contract.sh`: regression for schema-valid and broken tmp-summary reconcile bundle status-validation reports
- `resolve_federated_ci_summary_tmp_reconcile_bundle_status.py`: canonical consumer for tmp-summary reconcile bundle status + status-validation pairs
  - accepts either a bundle status report or its status-validation report and resolves one fail-closed bundle surface
- `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status.sh`: regression for status-backed, status-validation-backed, and stale-status-validation tmp-summary reconcile bundle status resolution
- `validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle.py`: validator for resolved tmp-summary reconcile bundle status-bundle artifacts
  - fail-closes when the stored resolved bundle drifts from canonical status/status-validation sidecar resolution
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_validation_contract.sh`: regression for schema-valid and drifted tmp-summary reconcile bundle status-bundle validation reports
- `doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle.py`: operator-facing diagnosis for the resolved tmp-summary reconcile bundle status-bundle artifact
  - resolves from the canonical status/status-validation pair or reads an explicit bundle file, refreshes the canonical status-bundle validation report, and fails closed under `--require-pass` when the resolved bundle drifts
- `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle.sh`: regression for healthy and drifted tmp-summary reconcile bundle status-bundle diagnosis paths
- `gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle.sh`: thin fail-closed gate wrapper around `doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle.py --require-pass`
- `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle.sh`: regression for passing and failing tmp-summary reconcile bundle status-bundle gate paths
- `validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status.py`: validator for the status-bundle doctor-owned status sidecar
  - fail-closes when the stored status doc drifts from canonical doctor output or its recorded validation output path
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_contract.sh`: regression for valid and invalid tmp-summary reconcile bundle status-bundle status documents
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_validation_contract.sh`: regression for schema-valid and broken tmp-summary reconcile bundle status-bundle status-validation reports
- `resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status.py`: canonical consumer for tmp-summary reconcile bundle status-bundle status + status-validation pairs
  - accepts either a status-bundle doctor status report or its status-validation report and resolves one fail-closed bundle surface
- `test_resolve_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status.sh`: regression for status-backed, status-validation-backed, and stale-status-validation tmp-summary reconcile bundle status-bundle status resolution
- `validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.py`: validator for resolved tmp-summary reconcile bundle status-bundle-status bundle artifacts
  - fail-closes when the stored resolved bundle drifts from canonical status-bundle doctor sidecar resolution
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_validation_contract.sh`: regression for schema-valid and drifted tmp-summary reconcile bundle status-bundle-status bundle validation reports
- `doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.py`: operator-facing diagnosis for the resolved tmp-summary reconcile bundle status-bundle-status bundle artifact
  - resolves from the canonical status-bundle-status/status-bundle-status-validation pair or reads an explicit bundle file, refreshes the canonical bundle-validation report and status/status-validation sidecars, and fails closed under `--require-pass` when the resolved bundle drifts
- `test_doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.sh`: regression for healthy and drifted tmp-summary reconcile bundle status-bundle-status bundle diagnosis paths
- `gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.sh`: thin fail-closed gate wrapper around `doctor_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.py --require-pass`
- `test_gate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle.sh`: regression for passing and failing tmp-summary reconcile bundle status-bundle-status bundle gate paths
- `validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status.py`: validator for the status-bundle-status bundle doctor-owned status sidecar
  - fail-closes when the stored status doc drifts from canonical doctor output or its recorded validation output path
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_contract.sh`: regression for valid and invalid tmp-summary reconcile bundle status-bundle-status bundle status documents
- `test_validate_federated_ci_summary_tmp_reconcile_bundle_status_bundle_status_bundle_status_validation_contract.sh`: regression for schema-valid and broken tmp-summary reconcile bundle status-bundle-status bundle status-validation reports
- `doctor_federated_ci_summary_tmp_reconcile.py`: operator-facing diagnosis for the latest tmp-summary reconcile report plus its validation report
  - doctor treats a mismatched reconcile-validation fingerprint as `validation state: stale`
  - doctor fails closed under `--require-pass` when tmp-summary reconcile validation is stale, missing, or failing
- `test_doctor_federated_ci_summary_tmp_reconcile.sh`: regression for healthy, restored, and stale-validation tmp-summary reconcile paths
- `gate_federated_ci_summary_tmp_reconcile.sh`: thin fail-closed gate wrapper around `doctor_federated_ci_summary_tmp_reconcile.py --require-pass`
- `test_gate_federated_ci_summary_tmp_reconcile.sh`: regression for passing and failing tmp-summary reconcile gate paths
- `test_federated_ci_workflow_summary_contract.sh`: regression that keeps the GitHub `federated-summary` job wired to `run_federated_summary_ci_check.sh` instead of ad-hoc inline summary generation
- `test_federated_ci_local_matrix_mode_contract.sh`: regression that keeps local federated CI pinned to the canonical `local` runtime profile while still requiring `provider-gateway-ready`
- `test_inventory_issue_lifecycle_audit_snapshot.sh`: strict regression that proves the checked-in inventory issue lifecycle audit snapshot still passes against the repo's canonical archive/manifests
- `validate_federated_ci_summary.py`: validator for stamped/latest federated CI summary artifacts
  - validation reports now carry the source summary fingerprint (`summary_run_id`, `summary_generated_at_utc`, `summary_verdict`) so downstream readers can reject stale validations
- `test_validate_federated_ci_summary_contract.sh`: regression for valid and invalid federated CI summary documents
- `assess_federated_ci_summary_readiness.py`: canonical readiness reporter that turns latest summary + validation into one `ready|blocked` artifact under `planningops/artifacts/validation/`
- `test_assess_federated_ci_summary_readiness.sh`: regression for ready and stale-validation readiness paths
- `validate_federated_ci_summary_readiness.py`: validator for federated CI summary readiness artifacts
- `test_validate_federated_ci_summary_readiness_contract.sh`: regression for valid and invalid readiness documents
  - readiness assessment now writes a companion validation report artifact, mirroring the stamped/latest summary + validation pair
- `doctor_federated_ci_summary.py`: operator-facing diagnosis for the latest stamped federated CI summary plus its validation, readiness, and tmp-summary reconcile evidence
  - doctor treats a mismatched validation fingerprint as `validation state: stale`, a mismatched readiness-validation fingerprint as `readiness validation state: stale`, and a mismatched tmp-summary reconcile validation fingerprint as `tmp-summary reconcile validation state: stale`
  - doctor fails closed under `--require-pass` when summary validation, readiness, readiness validation, tmp-summary reconcile, or tmp-summary reconcile validation is stale or missing
- `test_doctor_federated_ci_summary.sh`: regression for healthy and failing doctor output paths
- `gate_federated_ci_summary.sh`: thin fail-closed gate wrapper around `doctor_federated_ci_summary.py --require-pass`
- `test_gate_federated_ci_summary.sh`: regression for passing and failing gate paths
- `test_federated_ci_summary_contract_doc.sh`: regression that keeps the canonical contract doc aligned with required artifacts and commands
- `test_plain_python_compat_manifest_contract_doc.sh`: regression that keeps the plain-`python3` manifest contract aligned with resolver, validator, doctor, gate, and smoke commands
- `test_uap_automation_operations_summary_contract.sh`: regression that keeps the higher-level automation operations summary aligned with the federated CI readiness and supervisor handoff model
- canonical contract: `planningops/contracts/federated-ci-summary-contract.md`
- canonical contract: `planningops/contracts/plain-python-compat-manifest-contract.md`

Compatibility wrappers are kept at `planningops/scripts/*.py|*.sh`; `repo_execution_adapters.py` remains the legacy root name for `adapter_registry.py`.

## Change Rules
- New multi-repo execution entrypoints must be added here, not at `planningops/scripts/` root.
- Repo-specific execution mapping must stay here; core loop modules must consume this directory through thin interfaces only.
- Shared local bootstrap logic for federated checks should live here so shell runners and Python entrypoints reuse the same managed environment policy.
- Issue-to-mission normalization belongs here when planningops is orchestrating repo-owned runtime smoke.
- Root wrappers must remain thin dispatch-only files.
- Any federation entrypoint addition must update `planningops/config/repository-boundary-map.json` and pass `test_validate_repo_boundaries_contract.sh`.
- Local federated CI is allowed to hard-fail on sibling runtime gates when the promoted local planner path depends on them.
